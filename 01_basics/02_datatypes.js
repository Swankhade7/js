"use strict"; //treat all the code in this file in newer version

// alert(7 + 7) //This code wont run in node as there is a different syntax in node. This only works in browser based console

//data types
let name = "sanket";//string
let age = 29; //number
let isLoggedIn = true ; //boolean
let state; // underfined
let device = null; //standalone value

//symbol=> uniqueness
//object

console.log(typeof undefined); //undefined
console.log(typeof null); //object

